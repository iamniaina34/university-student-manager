import React, { useEffect } from 'react'
import { Typography } from '@mui/material';
import Heading from '../../components/Heading';

function Seances() {

    useEffect(() => {
        // Todo
    }, []);

    return (
        <React.Fragment>
            <Heading label={'Liste des séances'} />
        </React.Fragment>
    )
}

export default Seances