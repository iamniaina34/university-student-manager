import React, { useEffect } from 'react'
import { Typography } from '@mui/material';
import Heading from '../../components/Heading';

function Classes() {

    useEffect(() => {
        // Todo
    }, []);

    return (
        <React.Fragment>
            <Heading label={'Liste des classes'} />
        </React.Fragment>
    );
}

export default Classes